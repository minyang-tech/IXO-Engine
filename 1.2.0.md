# IXO Engine V1.2.0 업데이트 계획 및 구현 기준

V1.2.0은 V1.1.4를 건너뛰고 2026-06-27 정기 릴리즈로 진행하는 대규모 업데이트입니다.
목표는 IXO Engine을 단순 노드 데모가 아니라 실제 앱 제작 워크플로우에 바로 투입할 수 있는 편집기로 끌어올리는 것입니다.

## 핵심 방향

- Scene별 노드, UI, 지역변수를 분리합니다.
- Undo/Redo를 노드, UI, Scene, 변수, 속성 변경에 걸쳐 하나의 이력 스택으로 정리합니다.
- 버튼과 Custom Button에서 공통으로 쓰는 Action System을 정식화합니다.
- UI 스타일 편집, 스타일 프리셋, 제한 CSS 편집을 제공합니다.
- Export 산출물의 앱 이름, 아이콘, 창 제목, 창 크기, 배경색, Splash Screen 설정을 보존합니다.
- Export에는 실제 사용 중인 에셋만 포함하는 방향으로 정리합니다.
- IXO Engine으로 제작된 산출물에는 사용자에게 보이지 않는 제작 신호를 포함해 악용 추적 기반을 둡니다.

## 구현된 항목

### Scene System

- Scene 목록 순서를 보존합니다.
- Scene별 노드, UI, 지역변수를 분리합니다.
- Scene 생성, 이름 변경, 삭제 안전 확인을 제공합니다.
- Scene 복제를 추가했습니다.
- Scene 전환 시 미리보기 화면에 짧은 전환 애니메이션을 적용합니다.
- `Scene Start` 노드는 현재 실행 중인 Scene 정보를 기준으로 동작합니다.

### Undo/Redo

- 노드 이동, 삭제/생성, 연결 변경, UI 이동/크기 변경, 속성 변경, Scene 변경, 변수 변경을 같은 history stack에 저장합니다.
- 상단 작업 탭 영역에 되돌리기/다시 실행 버튼을 제공합니다.

### Action System

버튼 계열 UI에서 다음 동작을 선택할 수 있습니다.

- `Open URL`
- `Run Function`
- `Set Variable`
- `Toggle UI`
- `Go Scene`
- `Play Sound`
- `Request HTTPS`

네트워크가 필요한 액션은 기존 네트워크 동의, HTTPS 강제, 사설망/localhost 차단, 로그 마스킹 정책을 유지합니다.

### UI Styling

- 텍스트, 버튼, 입력창, 컨테이너, 카드 계열 UI에 스타일 프리셋을 적용할 수 있습니다.
- 기본 CSS, Hover CSS, Click CSS를 제한/검증 모드로 입력할 수 있습니다.
- 허용된 CSS 속성만 반영하고 `url()`, `@import`, `javascript:`, `data:` 같은 위험 패턴은 차단합니다.

### Export App Polish

- 앱 이름, 아이콘, 창 제목, 기본 창 크기, 리사이즈 여부, 배경색, Splash Screen 설정을 Export 설정으로 저장합니다.
- 내보낸 런타임에서는 편집기 헤더와 불필요한 미리보기 UI를 숨깁니다.
- 프로젝트에서 실제 참조되는 에셋만 Export 대상에 포함합니다.
- 산출물 내부에 `.ixo-origin.json`과 `_ixoBuildSignal` 제작 신호를 포함합니다.

### Asset Manager

- 이미지, 사운드, 아이콘, `.ixo`, 테마 파일을 관리합니다.
- 이미지 에셋 미리보기와 이름 변경을 지원합니다.
- 이미지 에셋은 Canvas/UI로 드래그할 수 있고, `.ixo` 에셋은 현재 Scene에 붙여넣을 수 있습니다.
- 미사용 에셋 정리 기능을 제공합니다.

### Templates

다음 시작 템플릿을 제공합니다.

- 빈 프로젝트
- 계산기
- 퀴즈 앱
- 미니 채팅 UI
- 랜딩 페이지
- 게임 HUD
- 로그인/프로필 화면
- 기존 샘플 템플릿

### Performance

- Canvas Builder 미리보기 렌더링은 `requestAnimationFrame`으로 묶고 화면 밖 UI는 Canvas 레이어에서 제외합니다.
- React Flow는 보이는 노드만 렌더링하도록 유지합니다.
- 런타임 변경 감지는 노드 시그니처 기반으로 dirty node 목록을 계산해 추후 부분 재계산 확장 기반을 둡니다.

## 유지되는 보안 정책

- 네트워크 계열 노드는 사용자 동의 후에만 실행합니다.
- 네트워크 요청은 HTTPS만 허용합니다.
- localhost, 사설망, 내부망 주소는 차단합니다.
- 외부 URL 로그는 마스킹합니다.
- 스크립트 노드는 제한 실행 모드를 기본값으로 사용합니다.
- 파일 감시 노드는 별도 사용자 승인을 요구합니다.

## QA 체크리스트

- V1.1.x 프로젝트 자동 마이그레이션
- Scene 전환, 복제, 삭제, 이름 변경
- Scene별 노드/UI/지역변수 격리
- Undo/Redo 회귀 테스트
- Action System 7종 실행 테스트
- 네트워크 액션 차단 정책 테스트
- Export 설정 보존 확인
- Export 산출물에서 편집기 흔적 제거 확인
- 템플릿 7종 생성 및 미리보기 확인
- Windows/Linux 빌드 확인
