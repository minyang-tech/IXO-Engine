# AGENTS.md

## 민양테크 표준 AI & 개발 에이전트 행동 지침
* **소유 및 관리자:** [@Whoasked](https://minyangtech.n-e.kr/@whoasked)
* **주의:** 관리자(`@Whoasked`) 이외의 기여자가 본 파일을 수정하거나, 본 파일의 규칙을 위반하는 Pull Request를 제출할 경우 즉시 거부(Reject)됩니다.

---

## 1. 프로젝트 필수 규칙 (Tech Stack & Workflow)

### 1.1 검증 및 빌드 파이프라인
- **린트 체크 필수:** 코드 작성 및 수정 완료 후, 테스트를 진행하기 전에 반드시 `pnpm -r lint` 및 `pnpm -r build`를 실행하여 에러와 경고(Warning)가 없는지 확인할 것.
- **테스트 코드:** 기존 테스트 스위트가 존재할 경우, 작업 후 `pnpm test`를 통해 회귀 테스트(Regression Test)를 통과하는지 확인할 것.

### 1.2 의존성(Dependencies) 및 API 관리
- **Public API 하위 호환성 유지:** 기존에 노출된 Public API(함수 시그니처, 엔드포인트, 데이터 구조 등)는 절대 변경하거나 삭제하지 말 것. 변경이 불가피한 경우 반드시 사전 승인을 요청할 것.
- **의존성 추가 제한:** 새로운 패키지나 외부 라이브러리를 추가할 때는 `pnpm add`를 실행하기 전, **[추가하려는 이유, 대체 불가능한 이유, 패키지 용량 및 보안 영향]**을 명확히 설명하고 사용자의 동의를 얻을 것.

### 1.3 아키텍처 및 리팩토링
- **디자인 패턴 준수:** 코드 수정 시 가독성과 확장성을 위해 디자인 패턴(예: 싱글톤, 팩토리, 전략 패턴 등)을 의도적으로 적용하고, 기존 프로젝트의 아키텍처 결을 유지할 것.
- **모노레포 구조 존중:** 패키지 간의 참조 관계를 확인하고, 순환 참조(Circular Dependency)가 발생하지 않도록 주의할 것.

---

## 2. 코드 스타일, 보안 및 문서화

### 2.1 문서화 및 주석 규칙
- **언어 통일:** 모든 코드 내 주석, JSDoc, commit 메시지, PR 본문은 **한국어**로 작성할 것.
- **동기화:** 코드를 수정할 때 해당 코드와 연관된 주석, README.md, 웹사이트 상 문서도 반드시 실시간으로 함께 업데이트할 것. 코드와 주석이 불일치하는 상황을 절대 만들지 말 것.

### 2.2 디버깅 및 정리
- **프로덕션 코드 청결:** 개발 중 추가한 임시 디버그 로그(`console.log`, `print` 등) 및 테스트용 더미 코드는 커밋(Commit) 전 반드시 전량 삭제할 것.

### 2.3 보안 및 시스템 안전성 (Critical Exception)
- **독단적 수정 금지:** 정상적인 상태에서 코드의 큰 틀(구조)을 무단으로 변경하거나 권한/보안 설정을 바꾸는 행위는 금지되며, 변경 전 상세 계획을 먼저 제시해야 함.
- **예외 조항 (보안 취약점):** 단, 시스템의 안정을 위협하는 **매우 심각한 수준의 보안 취약점(예: 인젝션, 권한 탈취 등)이나 치명적인 런타임 크래시 에러**가 발견된 경우, 이 제한 규칙을 무시하고 즉시 격리/수정 조치를 취한 뒤 보고할 수 있다.

---

## 3. 답변 및 상호작용 규칙 (Interaction Rules)

### 3.1 소통 언어 및 톤앤매너
- 질문에 대한 답변, 문제 해결 과정 설명, 에러 로그 분석 등 사용자와의 모든 상호작용은 **한국어**로 명확하고 간결하게 작성할 것.

### 3.2 작업 범위 제한 (Scope Control)
- **임의 수정 절대 금지:** 사용자가 지정하거나 요청한 작업 범위(Context) 밖의 파일은 절대 임의로 수정하거나 접근하지 말 것. 
- **연쇄 변경 제안:** 특정 작업으로 인해 범위 밖의 파일 수정이 불가피하게 연쇄적으로 필요한 경우, 수정을 진행하기 전 사용자에게 변경이 필요한 파일 목록과 이유를 브리핑하고 명시적 승인을 받을 것.

### 추가 요청 사항
- **빌드 파일을 통합할것** : 빌드파일을 dist_election_final/, release/, dist_final/ 같이 다른버전이 여러개로 분리되어있지않고, 단일 파일에 빌드파일을 넣을것.